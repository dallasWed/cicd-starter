# cicd-starter

ChatGPT was used to debug workflow error, which was solved when __init__.py files were included to both the src and tests folders.  
AI query used: "When I run my python code and the related test in terminal, they work. When I push the same code to github, the workflow fails with exit on code 2. What is happening here?".